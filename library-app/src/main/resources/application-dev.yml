spring:
  config:
    activate:
      on-profile: dev

  jpa:
    properties:
      hibernate:
        generate_statistics: true
        show_sql: true
        format_sql: true
  liquibase:
    contexts: dev


logging:
  level:
    org.example.library: DEBUG