spring:
  datasource:
    url: jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;MODE=PostgreSQL
  jpa:
    hibernate:
      ddl-auto: validate
  liquibase:
    enabled: true

app:
  recommendations:
    enabled: false

application:
  security:
    jwt:
      secret-key: "very-long-and-secure-test-secret-key-for-jwt-generation-32-chars"

frontend:
  url: "http://localhost:4200"